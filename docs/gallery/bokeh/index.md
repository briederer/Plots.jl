# Bokeh

To switch to the `Bokeh` backend, you can use:

```julia
using Plots
bokeh()
```

The demos are generated from `Plots._examples`. Empty demos are features that this backend does not support.

{{{democards}}}
